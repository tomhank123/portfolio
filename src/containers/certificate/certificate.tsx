import * as React from 'react';
import { FC } from 'react';

interface IProps {}

const CertificateContainer: FC<IProps> = (props: IProps) => {
  return (
    <h1>Container: Certificate</h1>
  );
};

export default CertificateContainer;
